# Grafana Dashboard Provisioning Configuration
apiVersion: 1

providers:
  - name: "PetBuddy Dashboards"
    orgId: 1
    folder: "PetBuddy"
    folderUid: "petbuddy"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
